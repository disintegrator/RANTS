import React from "react";
import styled from "styled-components";

const Container = styled.div`
  font-family: sans-serif;
  color: red;
`;

export default () => <Container>Welcome to next.js!</Container>;
